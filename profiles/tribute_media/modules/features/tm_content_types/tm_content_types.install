<?php

/**
 * @file
 * 
 */

// ==============================
// = Implements hook_install(). =
// ==============================

function tm_content_types_install() {
  // Enable webform on Landing Page content type
  $types = variable_get('webform_node_types', array('webform'));
  $types[] = 'landing_page';
  variable_set('weform_node_types', $types);
}
